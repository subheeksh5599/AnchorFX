# Environment Variables
# Copy this to .env and fill in values.
# The .env file is gitignored — never commit it.

# Stellar testnet mnemonic for CLI deploy scripts (12-24 words)
STELLAR_MNEMONIC=

# Soroban contract ID (optional — falls back to hardcoded testnet default)
CONTRACT_ID=

